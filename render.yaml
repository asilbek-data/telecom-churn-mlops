# Optional: lets Render create the service from this repo automatically.
services:
  - type: web
    name: churn-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeploy: true
